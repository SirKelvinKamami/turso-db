# Turso Service — Daily Report 2026-08-31

## Summary
"All of the above" batch: finished the webhook roadmap, added push-sync, cleaned git
history, and prepped the push. Working tree is otherwise fully committed.

## Work Completed
- **Retry/backoff queue** — failed/non-2xx webhook deliveries retried 1s/2s/4s/8s (5
  attempts), off the request path.
- **Row-level change events** — payload `changes` array with best-effort `op`
  (insert/update/delete/ddl/other) + `table` classifier (quoted ids, OR-behavior,
  IF NOT EXISTS, comments; CTEs → null).
- **Custom delivery headers** — per-webhook `headers` (validated) sent with delivery;
  enables `Authorization` for sync receivers.
- **Supabase mirror** — webhooks snapshot upserted to `turso_webhooks` when env set;
  cold-start load from Supabase when no local file.
- **Sync receiver** — `POST /v1/sync/{id}` applies webhook statements, tenant-checked,
  deliberately non-dispatching (one-hop ⇒ loop-free). Verified end-to-end: webhook →
  `/sync` replicated A→B.
- **Recipient helper** — `scripts/webhook-receiver.js` (zero-dep Node): HMAC
  verification, `/last` + `/count`, `BAD=N` mode.
- **Git history cleanup** — `git filter-branch` rewrote 54 commits + `gc --prune=now`;
  all four leaked literals gone from the object DB (full scan clean). Rewritten main is
  ahead 3 / behind 0 of origin/main → plain push works.
- **Docs/memory** — DEPLOY.md (sync API row, retries, changes, headers, push-sync how-to),
  SESSION_LOG, STATUS, this report.

## Tests
- `cargo test --all-targets`: **22 pass** (added classifier, header-validation, retry,
  give-up, custom-header/changes assertions).
- fmt/clippy/build clean.
- Live: signature verified at Node receiver; `BAD=2` → exactly 3 hits; multi-statement
  batch classified insert+update; A→B sync with no loop.

## Blocked / Decisions Needed
- **Push still withheld** — Render dashboard secrets not yet re-entered; credentials
  (JWT/admin/seed/Google) not yet rotated. After the boss does that, push is one command
  (fast-forward, no force).

## Next Actions
- Boss: set Render env vars (JWT_SECRET, ADMIN_PASSWORD, SEED_USERS, SUPABASE_URL,
  SUPABASE_SERVICE_KEY), rotate leaked creds, then say push.
- Future features queued: durable delivery queue, change capture w/ values, Supabase
  orphan cleanup, full libsql replica sync.