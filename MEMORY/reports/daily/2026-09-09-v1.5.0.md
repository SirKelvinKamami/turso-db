# Turso Service — Daily Report 2026-09-09 (v1.5.0)

## Summary
v1.5.0 batch: per-webhook custom retry policy (max_attempts + backoff_ms, validated and
echoed over the API) and UPDATE/DELETE row-level value capture alongside the existing
INSERT VALUES work. 69 tests pass locally; shipped via push → Render auto-deploy.

## Work Completed
- **Retry policy:** `RetryPolicy { max_attempts, backoff_ms }` on webhook create
  (`retry` field); drives the in-memory delivery burst per hook; pending-queue limits
  stay global env. Legacy webhook/pending files parse (serde default).
- **UPDATE/DELETE capture:** `changes[]` entries now get `values` for updates
  (`set` map + optional `where`) and deletes (`where`; null for full-table sweep).
  New top-level-aware tokenizers `split_top_level`/`find_top_level` + `keyword_pos`.
- **Bug found by new tests:** trimmed/untrimmed index mismatch in `parse_set_assignments`
  prefixed values with `= `; fixed.
- **Version/docs:** Cargo.toml 1.5.0, CHANGELOG 1.5.0, DEPLOY.md (payload example with
  update/delete `values`, retry policy docs), MEMORY updated.

## Tests
- `cargo test --all-targets`: **69 pass** (was 63).
- `cargo fmt --check`, `cargo clippy --all-targets`: clean.

## Blocked / Next
- Push v1.5.0 → GitHub Actions → Render auto-deploy; verify health endpoint version.
- Credential rotation still outstanding (manual, boss).
- Next candidates: full libsql replica sync (planning), true change-framing before
  execute, webhook PATCH endpoint.