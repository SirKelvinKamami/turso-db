# Turso Service — Daily Report 2026-08-29

## Summary
Shipped the **webhook/sync feature (v1.1.0)**: REST + libsql-pipeline writes now fire
signed `write` notifications to registered endpoints. Version bumped 1.0.0 → 1.1.0.

## Work Completed
- New `src/webhooks.rs`: file-backed `WebhookStore` (`DATA_DIR/webhooks.json`), URL/event
  validation, HMAC-SHA256 signing (`X-Turso-Signature`), fire-and-forget dispatch with
  10s timeout, payload with db id/name/owner/statements/rows.
- Wired to both write paths: `POST /v1/databases/{id}/execute` and libsql pipeline
  (`execute` + `batch`). Read-only statements excluded via new `sql_is_query` classifier.
- New API: `GET|POST /v1/databases/{id}/webhooks`, `DELETE /v1/databases/{id}/webhooks/{hook_id}`;
  DB delete clears its webhooks. Tenant isolation via `check_db_owner`.
- `db.execute` returns `ExecuteReport`; `split_sql` exposed `pub(crate)`.
- Added deps `hmac`/`sha2`/`hex`; version → 1.1.0; DEPLOY.md updated.

## Tests
- `cargo test --all-targets`: **16 tests, all pass** — including a live loopback
  delivery test (axum receiver) asserting signature/headers/payload.
- `fmt --check`, `clippy --all-targets`, `build`: all clean, 0 warnings.

## Manual smoke (localhost:3100)
Register (201) → list → bad URL/event → 400 → 2-statement batch execute returned in
~114ms (non-blocking dispatch) → delete 204 → re-delete 404 → webhooks.json cleaned on
DB deletion.

## Blocked / Decisions Needed
- Same as before: Render dashboard secrets must be re-entered manually before push;
  credentials should be rotated (old values were historically committed).
- Owner chose commit-only for 2026-08-28; push decision still pending for this feature.

## Next Actions
- Commit webhook feature locally (done as part of session end).
- Owner: re-enter Render secrets + rotate creds, then push.
- Future: row-level webhook events, retry queue, Supabase webhook persistence,
  full libsql replica sync.