# Turso Service — Daily Report 2026-08-28

## Summary
Resumed the turso-db service. Fixed the broken local Windows build, completed a security pass (Google OAuth verification, removed committed secrets, graceful config errors), made `/query` return real column names, and cleaned all rustfmt/clippy warnings. Ready for commit pending owner decisions.

## Work Completed
- Build env repaired (ReadOnly attribute cleared on 1273 dirs; toolchain paths recorded; `start.ps1` rewritten).
- Google login: JWKS-verified ID tokens (`src/google.rs`), `/v1/auth/google-token` + `/v1/auth/google-config`, dashboard button wired.
- `ADMIN_PASSWORD` missing now returns 500 `CONFIG_ERROR` (was a panic).
- Committed secrets scrubbed from `render.yaml`, `DEPLOY.md`, `admin.ps1`, `static/*.js`.
- `/query` returns real column names (`id,first_name,age` verified).
- `delete_database` now removes `-wal`/`-shm` companion files.
- **Multi-statement execute:** string/comment-aware `split_sql` + per-statement run, message `ran N statements, X rows affected`. Verified live (3 stmts). 8 unit tests added.
- Port conflict resolved for local dev: `BIND_ADDRESS=0.0.0.0:3100` in `.env` (untracked).
- `cargo fmt`, `cargo clippy --all-targets`, `cargo build`, `cargo test` — all clean (0 warnings).
- Memory structure created: `MEMORY/projects/turso-db/`, `MEMORY/lessons/`, `MEMORY/patterns/`, `MEMORY/reports/daily/`.
- **Committed locally** as `bf74f20` (no push, per owner). Note: an initial local commit contained the old secret literals in the session log; the commit was amended (local-only, never pushed) to remove them, and `git grep -F` confirms HEAD is clean.

## Tests
- `cargo test --all-targets`: 8 tests (new `split_sql` unit tests), all pass.
- Manual smoke test (port 3100): health, google-config, admin login, create/execute/query (real columns), multi-statement execute, delete — all passed.

## Blocked / Decisions Needed
1. Render dashboard secrets must be re-entered manually (render.yaml `sync: false`) — required before any push-triggered deploy.
2. No push yet — owner chose "commit only".
3. Credentials should be rotated (old values were once committed).

## Next Actions
- Re-enter Render secrets, then push when ready.
- Pick the next feature (owner hinted webhook/sync).
- More unit tests (admin/plan/rate-limit); consider multi-statement in `/query`.