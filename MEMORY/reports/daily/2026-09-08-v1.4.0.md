# Turso Service — Daily Report 2026-09-08 (v1.4.0)

## Summary
v1.4.0 batch: bounded pending-delivery queue (max-attempts + TTL), HTTP route-handler
integration tests, row-level `values` capture for INSERT, Config-in-AppState refactor,
CI exploration. 63 tests pass locally; shipped via push → Render auto-deploy.

## Work Completed
- **Bounded retry queue:** `WEBHOOK_PENDING_MAX_ATTEMPTS` (10080) + `WEBHOOK_PENDING_TTL_SECS`
  (604800) drop exhausted/expired pending deliveries; `created_at` stamped at persist;
  legacy files parse via serde default.
- **Integration tests (routes.rs `mod integration_tests`):** in-process Router via
  tower `TestClient` — auth guards, login+DB lifecycle, 404-vs-401 ordering, ownership
  isolation, webhook validation, plan-based per-user rate limit, one-time setup.
- **VALUES capture:** `capture_insert_values` → `{columns, rows}`; `changes[]` entries
  carry `values` for `insert` ops (null for `INSERT ... SELECT`).
- **Config-in-AppState:** preloaded `Config` in state; handlers stop re-reading
  dotenv/env per request (hermetic tests).
- **Version/docs:** Cargo.toml 1.4.0, CHANGELOG 1.4.0 entry, DEPLOY.md payload
  example + queue env vars, MEMORY STATUS/SESSION_LOG updated.

## Tests
- `cargo test --all-targets`: **63 pass** (was 49).
- `cargo fmt --check`, `cargo clippy --all-targets`: clean.

## Blocked / Next
- Push v1.4.0 → GitHub Actions → Render auto-deploy; verify health endpoint version.
- Credential rotation still outstanding (manual, boss).
- Future: UPDATE/DELETE value capture, full libsql replica sync.