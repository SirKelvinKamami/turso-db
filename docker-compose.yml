version: '3.8'

services:
  turso-service:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - RUST_LOG=turso_service=debug
    restart: unless-stopped
